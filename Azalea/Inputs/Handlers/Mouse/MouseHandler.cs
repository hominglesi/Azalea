using System.Numerics;

namespace Azalea.Inputs.Handlers.Mouse;

public class MouseHandler : InputHandler
{
	private Vector2? lastPosition;

	/*
    public override bool Initialize(GameHost host)
    {
        if (base.Initialize(host) == false)
            return false;

        if ()
    }*/
}
